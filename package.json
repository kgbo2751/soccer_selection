{
  "name": "soccer-selection-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "start": "npm run dev",
    "server": "node backend/server.js",
    "client": "npm start --prefix client",
    "build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "concurrently": "^8.2.2",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mongoose": "^7.5.0"
  },
  "engines": {
    "node": "18.x"
  },
  "devDependencies": {
    "cross-env": "^10.1.0"
  }
}
